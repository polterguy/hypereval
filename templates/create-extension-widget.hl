/*
 * Creates our _"create extension widget"_ snippet template, which you can choose
 * as a _"template"_ snippet when you create a new snippet.
 */


/*
 * _"New startup extension widget template"_ file.
 *
 * This event simply creates an extension widget, which simply says _"Hello World!"_,
 * that you can consume in other places of your project.
 */
create-event:foo.widgets.bar
  return
    literal
      innerValue:Hello World!!
