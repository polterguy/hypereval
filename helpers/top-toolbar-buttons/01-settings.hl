/*
 * Settings toolbar button. Shows a modal window, allowing the user to modify his settings.
 */
button
  innerValue:@"<span class=""icon-cog""></span>"
  title:Settings
  onclick

    /*
     * Showing settings for CodeMirror module to user.
     */
    micro.codemirror.settings
